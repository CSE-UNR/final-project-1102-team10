

	void printMadlib(char madlib[][100], int lines) {
	  for (int i = 0; i < lines; i++) {
	  printf("s\n", madlib[i]);
	  }
	  }
	  
	  void closeFile(File *file) {
	  fclose(file);
	  }

return 0;
}
